name = "PerfectPan/svgo-cli"

version = "0.1.0"

import {
  "PerfectPan/svgo@0.1.0",
}

readme = "../../README.mbt.md"

repository = "https://github.com/PerfectPan/svgo.mbt"

license = "MIT"

keywords = [ "svg", "svgo", "optimizer", "cli" ]

description = "svgo.mbt command-line interface: optimize SVG files from the terminal"

preferred_target = "native"
