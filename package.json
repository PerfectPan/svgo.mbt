{
  "name": "svgo.mbt-workspace",
  "private": true,
  "description": "svgo.mbt monorepo: MoonBit module (svgo/), npm package (packages/svgo-mbt), comparison suite (packages/compare), website (app/)",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -C app dev",
    "build:wasm": "scripts/build-wasm.sh",
    "compare": "pnpm -C packages/compare run all",
    "app": "moon -C svgo doc && node app/build.mjs",
    "verify": "scripts/verify.sh"
  }
}
